obs = obslua

function script_description()
  return "Gesell: OBS Lua skill template (basic)"
end

function script_load(settings)
  obs.script_log(obs.LOG_INFO, "Loaded Gesell basic OBS Lua template")
end
